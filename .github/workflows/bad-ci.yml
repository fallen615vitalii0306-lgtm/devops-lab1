name: Bad CI

on:
  push:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Lint (ignored)
        run: |
          echo "Lint failed"
          exit 1
        continue-on-error: true

      - name: Test
        run: echo "Tests passed"

      - name: Build
        run: echo "Building..."

      - name: Deploy (always runs)
        if: always()
        run: |
          echo "Deploying..."
          echo "TOKEN=super-secret-token"
